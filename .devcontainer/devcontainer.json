{
  "image": "captainbicboi/basic-image",
  "customizations": {
    "vscode": {
      "extensions": [
        "streetsidesoftware.code-spell-checker",
        "ms-vscode.node-debug2",
        "dbaeumer.vscode-eslint"
      ]
    }
  },
  "forwardPorts": [3000],
  "postCreateCommand": "apt-get update && apt-get install -y curl && curl -sS https://webi.sh/gh | sh && npm install && git checkout -b fix/my-fix-branch || echo 'Branch already exists or error occurred'"}
